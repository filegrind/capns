{
  "urn": {
    "tags": {
      "action": "extract_metadata",
      "ext": "md",
      "in": "content-type: text/markdown; profile=https://capns.org/schema/markdown-document",
      "out": "content-type: application/json; profile=https://capns.org/schema/file-metadata"
    }
  },
  "command": "extract-metadata",
  "title": "Extract Markdown Document Metadata",
  "cap_description": "Extract markdown document metadata including title, file size, word count, and other properties",
  "metadata": {},
  "accepts_stdin": true,
  "arguments": {
    "required": [
      {
        "name": "file_path",
        "arg_type": "string",
        "arg_description": "Path to the markdown file to process",
        "cli_flag": "file_path",
        "position": 0,
        "validation": {
          "pattern": "^[^\\0]+$",
          "min_length": 1
        }
      }
    ],
    "optional": [
      {
        "name": "output",
        "arg_type": "string",
        "arg_description": "Write output to specified file instead of stdout",
        "cli_flag": "--output",
        "validation": {
          "pattern": "^[^\\0]+$"
        }
      }
    ]
  },
  "output": {
    "output_type": "object",
    "output_description": "Structured metadata including file properties and markdown-specific metadata",
    "content_type": "application/json"
  }
}