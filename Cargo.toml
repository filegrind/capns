[package]
name = "capdef"
version = "0.1.0"
edition = "2021"
description = "Core cap identifier and definition system for LBVR plugins"
authors = ["LBVR Team"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.0"